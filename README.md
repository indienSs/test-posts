React + NestJS + PostgreSQL проект с Docker контейнеризацией.

## Быстрый старт

1. Создайте .env файл из примера:
cp .env.example .env

2. Запустите Docker контейнеры:
docker-compose up -d

3. Доступ к приложению:
Клиент: http://localhost:5173
Сервер API: http://localhost:3000/api